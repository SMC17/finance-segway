# 20 Project Finance

**Archetype:** `PROJECT_FINANCE_template.xlsx` (template copied)

Construction drawdown + IDC, operating CFADS, DSCR schedule vs. covenant.

Instances live in `/projects/`. Copy the template, rename to ticker or deal code, fill in Cover tab, run weekly_refresh_check.py.
