# 08 Asset Management

**Archetype:** `AM_template.xlsx` (template copied)

Fund NAV build, fee waterfall (mgmt fee + carry + hurdle + GP catch-up), performance attribution.

Instances live in `/funds/`. Copy the template, rename to ticker or deal code, fill in Cover tab, run weekly_refresh_check.py.
