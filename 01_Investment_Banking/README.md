# 01 Investment Banking

**Archetype:** `_template.xlsx` (template copied)

3-statement + DCF + comps. Base corp-finance model, sector modules layered on.

Instances live in `/companies/`. Copy the template, rename to ticker or deal code, fill in Cover tab, run weekly_refresh_check.py.
