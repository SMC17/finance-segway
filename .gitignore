# Excel lock/temp files
~$*.xlsx
*.tmp
# Scratch dirs created by verify_public_case_status.py / verify_m3_readiness.py,
# which copy each workbook into the repo root to recalculate it.
tmp*/

# Weekly refresh reports (generated — commit via CI, not manually, to avoid noise)
refresh_report_*.csv

# Python
__pycache__/
*.pyc
.venv/

# OS
.DS_Store

# L3 agent tool scratch output (demo/test instances) -- never the real
# evidence corpus under */instances/, see tools/agents/*.py
.agent-tool-scratch/

# build_all_models.py's parity-comparison scratch output -- generated fresh
# every run from the committed builders + committed templates it compares
# against, so it carries no information the repo doesn't already have.
# Accidentally committed once (see #55's follow-up cleanup); never again.
.model-build/
builder-parity-report.json
final-public-evidence-report.json
all-domain-public-evidence-report.json
frontier-program-report.json
evidence-receipt-integrity-report.json
m1-domain-hardening-report.json

# Raw downloaded source files (Damodaran .xls/.xlsx) cached by
# damodaran_public_facts.py before parsing -- regenerable from the source
# URL every run. The derived JSON snapshot + source_register CSV under
# tools/data_fabric/out/ (not this subdir) are the committed provenance
# artifact, per tools/data_fabric/README.md.
tools/data_fabric/out/_damodaran_raw/
