# 21 Fixed Income Rates

**Archetype:** `FIXED_INCOME_template.xlsx` (template copied)

Bond pricing (PV-based), modified duration/convexity (shock method), yield curve/2s10s.

Instances live in `/instruments/`. Copy the template, rename to ticker or deal code, fill in Cover tab, run weekly_refresh_check.py.
