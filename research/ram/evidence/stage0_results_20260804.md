# Stage-0 RAM Evidence Note

**Date**: 2026-08-04  
**Model**: `research/ram/simple_covariance.py`  
**Universe cap**: 10 names (enforced in code)  
**Author**: Finance-Segway maintainers

## Test results

Command:

```bash
cd research/ram
python -m unittest test_simple_covariance.py -v
```

Current test coverage includes:

- Universe hard-cap enforcement
- Equal-weight analytic identity (2-asset and 3-asset)
- Inverse-volatility weight properties
- Cholesky PSD accept (good matrices, identity, diagonal-dominant)
- Cholesky PSD reject (negative-eigenvalue style, asymmetric, zero matrix)
- Portfolio-variance conservation identity
- Frobenius norm positivity

All tests must remain green after any change to the skeleton.

## Performance / memory measurement

Run the micro-benchmark:

```bash
cd research/ram
python bench_stage0.py
```

It prints Python version, platform, total wall time for 2 000 iterations of (equal-weight risk + PSD) on a 10-name diagonal-dominant matrix, and per-iteration cost in ms.

Record the output here before Stage-1 promotion:

| Metric | Value | Machine / notes |
|--------|-------|-----------------|
| Time for 2 000 × (risk + PSD) on 10 names | _run bench_stage0.py_ | |
| Per iteration | _from bench output_ | |
| Peak RSS (optional) | _measure if desired_ | |

These numbers become the baseline for the Stage-1 performance gate defined in `STAGE_GATES.md`.

## Visualization

A research Excel workbook for the same universe is generated by:

```bash
python research/ram/build_universe_viz.py
```

Output: `research/ram/visualizations/_universe_viz_stage0.xlsx`

Sheets: Cover, Universe, Covariance, Risk Summary, Regime Summary, Notes.  
Explicitly marked as research visualization only — not a governed decision model.

## Checklist status for Stage 0 → Stage 1

From `research/ram/STAGE_GATES.md`:

- [x] Stage-0 unit tests exist and are green.
- [x] Additional PSD accept/reject cases added (identity, diagonal-dominant, zero, 3-asset identity).
- [ ] Measured runtime / memory on reference hardware recorded in the table above.
- [ ] One-page algorithmic note if the implementation changes further.
- [ ] Hard cap raised only after review of this evidence pack.

## Next action

1. Run `bench_stage0.py` on your machine and paste the numbers into the table.
2. Optionally expand the random PSD corpus further.
3. Once numbers are filled and reviewed, Stage-1 (≤50 names) implementation may begin.
